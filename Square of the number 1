#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
        int n,mul;
        scanf("%d",&n);
    if(n>0&&n<=9)
    {
        mul=n*n;
        printf("Square of %d is %d",n,mul);
    }
    else
    {
        printf("Invalid Input");
    }
    return 0;
}

