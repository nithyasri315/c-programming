#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int num,dig1,dig2,sum;
    scanf("%d",&num);
    dig1=num/10;
    dig2=num%10;
    sum=dig1+dig2;
    printf("Bird said:%d\n",num);
    printf("Alice must go in path-%d\n",sum);
    return 0;
}
